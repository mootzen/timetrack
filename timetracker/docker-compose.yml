
version: "3.8"

services:
  timetracker:
    build: .
    container_name: timetracker
    ports:
      - "8084:80"
    volumes:
      - ./data:/data
      - .:/app
    env_file:
      - .env
    restart: unless-stopped
